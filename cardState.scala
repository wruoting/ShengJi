
  object cardState extends Enumeration {
      //type cardstate can be various values, hence this declaration
      val InDeck,OnBoard, FaceDown, PlayedCards, P1Hand, P2Hand, P3Hand, P4Hand = Value
  }
